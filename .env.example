# Cloze API Credentials
CLOZE_USER_EMAIL=your_email@example.com
CLOZE_API_KEY=your_api_key

# Debug Mode (true/false)
DEBUG_CLOZE=false

# MCP Mode (true when used with Claude Desktop)
MCP_MODE=true

# Logging Level (debug, info, warn, error)
LOG_LEVEL=info